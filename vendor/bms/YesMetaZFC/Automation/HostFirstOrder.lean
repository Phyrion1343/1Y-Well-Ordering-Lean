import YesMetaZFC.Automation.HostFirstOrder.Backend

/-!
# 宿主一阶自动化入口

公开入口只聚合索引语法、直接语义、checked replay 后端和元层 provider。绑定深度由
`Fin depth` 进入语法类型，旧的 `WellSorted`、`Scope`、`Admissible` 桥不再导出。
-/

namespace YesMetaZFC
namespace Automation
namespace HostFirstOrder

end HostFirstOrder
end Automation
end YesMetaZFC
